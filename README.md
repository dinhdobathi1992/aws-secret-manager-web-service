# Secrets Console

A Next.js console for AWS Secrets Manager across multiple accounts, with Entra ID sign-in and per-account roles.

> Work in progress: this is being rebuilt from the previous Flask app. Full docs land with the final phase.

```bash
pnpm install
cp .env.example .env   # fill in
pnpm dev
```
